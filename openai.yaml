openapi: 3.0.1
info:
  title: Notion Task API
  version: "1.0.0"
paths:
  /:
    get:
      summary: Get all tasks from Notion DB
      operationId: getTasks
      responses:
        '200':
          description: A list of tasks from Notion
          content:
            application/json:
              schema:
                type: object
servers:
  - url: https://notion-task-api.even-raining.workers.dev
  